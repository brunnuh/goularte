import 'package:flutter/material.dart';

class OptionsArt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 60,
      color: Theme.of(context).appBarTheme.color,
    );
  }
}
